<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:afw_project.View_Model"
             x:Class="afw_project.Customer_Orders"
             Title="Your orders">
    <ContentPage.BindingContext>
        <local:VM_Customer_Orders/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <ListView x:Name="orderList" ItemsSource="{Binding Orders}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Grid>
                                <Label x:Name="order_id"/>
                                <Label x:Name="order_time"/>
                                <Label x:Name="order_state"/>
                                <Label x:Name="order_price"/>
                            </Grid>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>